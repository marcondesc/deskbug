@model ListPriorityViewModel

<div class="row">
    <div class="col text-center">
        <h1>@ViewData["Title"]</h1>
    </div>
</div>

<div class="row">
    <div class="col">
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Name</th>
                    <th scope="col">Grade</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var priority in Model.Priorities)
                {
                    <tr>
                        <th scope="row">@priority.Id</th>
                        <td>@priority.Name</td>
                        <td>@priority.Grade</td>
                        @* <td>

                            <a asp-controller="Priority" asp-action="Edit" asp-route-id="@priority.Id"
                            class="btn btn-sm btn-warning"><i class="fa-solid fa-pen-to-square"></i> Editar</a>
                            <a asp-controller="Priority" asp-action="Delete" asp-route-id="@priority.Id"
                            onclick="return confirm('Are you shure you want to delete this project?')"
                            class="btn btn-sm btn-danger"><i class="fa-regular fa-square-minus"></i> Excluir</a>
                            </td> *@
                    </tr>
                }


            </tbody>
        </table>
    </div>
</div>

@* <div class="row">
    <div class="col">
    <a asp-controller="Priority" asp-action="Create" class="btn btn-primary">New Priority</a>
    </div>
    </div> *@
